name: Makefile CI

on: push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Install dependencies
      run: make re

    - name: Run check
      run: make test

    - name: Run distcheck
      run: make restack

    - name: Run check
      run: make test